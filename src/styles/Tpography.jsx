import { textAlign } from '@mui/system';
import { myPalette } from './Palette';

export const myTpography = {
    h1: {
        fontSize: 'clamp(36px, 4vw, 56px)',
        fontWeight: 400,
        lineHeight: 1.2,
    },
};
